using UnityEngine;

// 此脚本已废弃 — 所有 YooAsset 初始化已在 MyYooAsset.Start() 中完成。
// 保留文件以维持场景引用结构。
public class Load : MonoBehaviour
{
    private void Awake()
    {
        // 不再执行任何初始化逻辑
        return;
    }
}
